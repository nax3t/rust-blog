[package]
name = "rust-blog"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.29.0", features = ["bundled"] }
anyhow = "1.0"

[dev-dependencies]
tempfile = "3.8.1"  # For creating temporary test databases
